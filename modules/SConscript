#!/usr/bin/env python
Import('build')
import os

modules = ['api', 'foo', 'python', 'foopy', 'javascript', 'foojs','ruby']

for module in modules:
	module_dir = build.dir + '/modules/' + module
	SConscript(module + '/SConscript', build_dir=module_dir, duplicate=0)

	out_dir = build.dir
	script = os.path.join(build.kroll_build_dir, 'module.py')
	os.system('%s %s %s %s' % (script, out_dir, module_dir, build.os))


