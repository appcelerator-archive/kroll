#!/usr/bin/env python
Import('build')

modules = ['api', 'foo', 'python', 'foopy', 'javascript']
#
for module in modules:
	SConscript(module+'/SConscript', build_dir=build.kroll_build_dir + '/modules/' + module, duplicate=0)
