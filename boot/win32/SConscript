#!/usr/bin/env python
Import('build')

Env = build.env.Clone()
Env.Append(CPPDEFINES={'WIN32_LEAN_AND_MEAN': 1})
Env.Append(CPPPATH=['#common/win32','#common'])
Env.Program(build.dir + '/boot', ['boot.cpp'],LIBS=['shell32', 'user32','kroll'])
