#!/usr/bin/env python
Import('build')

Env = build.env.Clone()
Env.Append(CPPDEFINES={'WIN32_LEAN_AND_MEAN': 1})
Env.Append(CPPPATH=['#common/win32'])
Env.Append(LIBS = ['user32', 'shell32'])
Env.Program(build.dir + '/kinstall', ['installer.cpp', '../api/file_utils.obj', '../api/unzip/unzip.obj'])
